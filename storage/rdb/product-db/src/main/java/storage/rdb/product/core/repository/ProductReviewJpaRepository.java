package storage.rdb.product.core.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductReviewJpaRepository extends JpaRepository<ProductReviewJpaRepository, Long> {
}