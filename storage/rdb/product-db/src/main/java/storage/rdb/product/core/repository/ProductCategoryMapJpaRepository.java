package storage.rdb.product.core.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductCategoryMapJpaRepository extends JpaRepository<ProductCategoryMapJpaRepository, Long> {
}