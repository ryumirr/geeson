spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_db # @todo 멀티 디비로 수정해야함. 엔티티 이것저것 이용하기 때문
    username: root
    password: "rootpass^^*"
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        ddl-auto: none
        dialect: org.hibernate.dialect.MySQL8Dialect
      packages-to-scan: domain.order.entity
  kafka:
    bootstrap-servers: localhost:9092
grpc:
  server:
    port: 6565
    address: 0.0.0.0
uuid:
  node-id: ${NODE_ID:1}
